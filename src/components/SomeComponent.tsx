import React from 'react';

const SomeComponent = () => {
    console.log('SomeComponent');
    return (
        <div>
            this is some component
        </div>
    );
};

export default SomeComponent;
